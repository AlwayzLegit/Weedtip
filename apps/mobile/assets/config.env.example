# Copy to assets/config.env (bundled as a Flutter asset; same Supabase project as web).
# NOTE: must NOT be a dotfile — Flutter's asset bundler skips leading-dot filenames.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-publishable-key
MAPBOX_TOKEN=
