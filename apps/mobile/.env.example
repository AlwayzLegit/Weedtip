# Flutter app environment (bundled as an asset via flutter_dotenv).
# Copy to apps/mobile/.env and fill in. Same project as the web app's anon key.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-publishable-key
MAPBOX_TOKEN=pk.your-mapbox-public-token
